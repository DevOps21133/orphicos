# Secrets - every .env at any depth (server/.env holds the provider key)
.env
**/.env

# Client local config (copied from client/config.example.toml)
client/config.toml

# Vendored engine (cloned in Phase 1, pinned in docs/engine-version.txt)
engine/UFO/

# Python
.venv/
__pycache__/
*.pyc

# Model weights
models/

# Logs
*.log
logs/
