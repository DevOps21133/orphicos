"""The OrphicOS shell — the product skin (CLAUDE.md §9).

A local dark-themed web UI (command bar, live log, kill switch, approval gate) that
drives the Phase 2 thin-client loop. It runs entirely on the user's machine and talks
only to SERVER_BASE; no LLM key or provider lives here (Rules 1 & 6).
"""
