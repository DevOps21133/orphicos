# OrphicOS thin-client runtime dependencies.
#
# The client uses ONLY windows-use's headless Desktop (UI-tree perception + action),
# httpx (transport to the OrphicOS brain), and FastAPI/uvicorn (the local Phase 5 shell
# UI). It loads NO LLM provider SDK and NO telemetry — the wall is enforced at runtime
# in client/_engine.py (CLAUDE.md Rule 1). FastAPI (MIT) and uvicorn/Starlette (BSD-3)
# are all permissive; recorded in THIRD-PARTY-NOTICES.txt.
#
# ACCEPTED DEBT: windows-use 0.8.1 pulls a large transitive tree that includes
# GPL/LGPL and provider/telemetry packages the client never loads. The exact
# installed set is pinned in client/requirements-lock.txt. Before ship (Phase 6),
# windows-use is replaced by a minimal vendored subset — see THIRD-PARTY-NOTICES.txt.

windows-use==0.8.1
httpx>=0.28,<1.0
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# Phase 3 voice input — LOCAL speech-to-text (mic audio never leaves the machine).
# av is pinned <14: newer av wheels ship DLLs that Windows Smart App Control blocks
# on locked-down machines (low binary reputation); 13.x loads cleanly.
faster-whisper>=1.2,<2.0
sounddevice>=0.5,<1.0
av>=13.1,<14
