# Copy to .env and fill in real values. NEVER commit .env.
# Local model server (OpenAI-compatible API root, e.g. vLLM/LM Studio) — no cloud keys.
# The engine's OpenAI client appends /chat/completions itself.
LOCAL_MODEL_BASE=http://localhost:1234/v1
LOCAL_MODEL_NAME=
